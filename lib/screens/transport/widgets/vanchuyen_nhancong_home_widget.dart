import 'package:flutter/material.dart';

class VanTaiNhanCongScreen extends StatelessWidget {
  const VanTaiNhanCongScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Vận tải - Nhân công'),
    );
  }
}
